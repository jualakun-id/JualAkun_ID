# Environment
.env
.env.local
.env.*.local

# Supabase CLI temp
supabase/.temp/

# Wrangler local cache (jangan commit auth tokens / build artifacts)
.wrangler/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
*.swp
*.swo

# Backup lokal thumbnail produk — JANGAN commit (besar + binary, sumber utama
# adalah Supabase Storage bucket `product-thumbnails`).
thumbnails-backup/
